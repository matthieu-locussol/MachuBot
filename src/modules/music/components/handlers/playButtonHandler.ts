import type { ButtonComponentHandler } from '../../../../types/components';

export const playButtonHandler: ButtonComponentHandler = async (interaction) => {
   interaction.reply('playButtonHandler');
};
